## How to use
```
Buat database dengan nama "stockbarang" (sesuaikan dengan isi .env file)
composer update / composer install
composer dumpt-autoload jika ada error / menambahkan new file
php artisan migrate
php artisan db:seed
php artisan key:generate
php artisan serve
```